<odoo>
    <record id="view_assetflow_allocation_tree" model="ir.ui.view">
        <field name="name">assetflow.asset.allocation.tree</field>
        <field name="model">assetflow.asset.allocation</field>
        <field name="arch" type="xml">
            <tree>
                <field name="asset_id"/>
                <field name="partner_id"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_assetflow_allocation_form" model="ir.ui.view">
        <field name="name">assetflow.asset.allocation.form</field>
        <field name="model">assetflow.asset.allocation</field>
        <field name="arch" type="xml">
            <form string="Asset Allocation">
                <sheet>
                    <group>
                        <field name="asset_id"/>
                        <field name="partner_id"/>
                        <field name="date_start"/>
                        <field name="date_end"/>
                        <field name="state"/>
                        <field name="notes"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_assetflow_allocations" model="ir.actions.act_window">
        <field name="name">Asset Allocations</field>
        <field name="res_model">assetflow.asset.allocation</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
